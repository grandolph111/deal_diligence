# Server
PORT=3001
NODE_ENV=development

# Database
DATABASE_URL="postgresql://user:password@localhost:5432/dealdiligence?schema=public"

# Auth0
AUTH0_AUDIENCE=https://api.dealdiligence.ai
AUTH0_ISSUER_BASE_URL=https://YOUR_TENANT.auth0.com

# CORS
FRONTEND_URL=http://localhost:3000

# =============================================================================
# AWS S3 Configuration (for VDR document storage)
# =============================================================================
# If S3_BUCKET is not set, the system runs in "mock S3 mode" for development.
# Mock mode stores files in-memory and doesn't persist uploads.
#
# To use real S3, configure the following:
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
S3_BUCKET=dealdiligence-vdr-dev

# Presigned URL expiry in seconds (default: 3600 = 1 hour)
S3_PRESIGNED_URL_EXPIRY=3600

# =============================================================================
# S3 Bucket Setup Instructions
# =============================================================================
# 1. Create S3 bucket with server-side encryption:
#    - Encryption: AES-256 (SSE-S3) or AWS KMS
#    - Block all public access
#    - Versioning: Optional but recommended
#
# 2. Configure CORS policy on the bucket:
#    {
#      "CORSRules": [
#        {
#          "AllowedOrigins": ["http://localhost:3000", "https://your-domain.com"],
#          "AllowedMethods": ["GET", "PUT", "POST", "DELETE", "HEAD"],
#          "AllowedHeaders": ["*"],
#          "ExposeHeaders": ["ETag", "Content-Length", "Content-Type"],
#          "MaxAgeSeconds": 3600
#        }
#      ]
#    }
#
# 3. Create IAM user with minimal permissions:
#    {
#      "Version": "2012-10-17",
#      "Statement": [
#        {
#          "Effect": "Allow",
#          "Action": [
#            "s3:GetObject",
#            "s3:PutObject",
#            "s3:DeleteObject",
#            "s3:HeadObject"
#          ],
#          "Resource": "arn:aws:s3:::YOUR_BUCKET_NAME/*"
#        },
#        {
#          "Effect": "Allow",
#          "Action": ["s3:HeadBucket"],
#          "Resource": "arn:aws:s3:::YOUR_BUCKET_NAME"
#        }
#      ]
#    }
#
# 4. Set lifecycle policy for cost optimization (optional):
#    - Transition to Glacier after 90 days for archived deals
#    - Delete incomplete multipart uploads after 7 days
# =============================================================================

# =============================================================================
# BerryDB Configuration (for AI-powered search) - Optional for MVP
# =============================================================================
# If not configured, the system falls back to PostgreSQL-based search.
BERRYDB_API_KEY=your_berrydb_api_key_here
BERRYDB_PROJECT_ID=your_berrydb_project_id_here

# Python Microservice (BerryDB bridge)
PYTHON_SERVICE_URL=http://localhost:8000

# =============================================================================
# Claude (Anthropic) — required for extraction, risk reports, chat
# =============================================================================
# Dev: use the direct Anthropic API. Prod: switch to AWS Bedrock.
# If CLAUDE_PROVIDER and/or ANTHROPIC_API_KEY are blank, the pipeline runs in
# MOCK mode — uploads accept and move through states but no real extraction
# happens. Full flows need a real key.

CLAUDE_PROVIDER=anthropic
ANTHROPIC_API_KEY=sk-ant-...

# Optional model overrides (defaults baked into config/index.ts are fine)
# CLAUDE_MODEL_EXTRACTION=claude-opus-4-7
# CLAUDE_MODEL_REPORT=claude-opus-4-7
# CLAUDE_MODEL_CHAT=claude-haiku-4-5
# CLAUDE_MODEL_RECONCILIATION=claude-sonnet-4-6

# Bedrock-only (prod). Leave blank in dev.
# CLAUDE_BEDROCK_MODEL_EXTRACTION=
# CLAUDE_BEDROCK_MODEL_REPORT=
# CLAUDE_BEDROCK_MODEL_CHAT=
# CLAUDE_BEDROCK_MODEL_RECONCILIATION=

# Extraction thinking budget (tokens). 0 disables extended thinking.
# CLAUDE_EXTRACTION_THINKING_BUDGET=2000
